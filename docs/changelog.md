# Changelog

Хронология значимых версий Miami Graphics. Подробности в `git log` и Supabase `app_versions`.

Записи идут по убыванию номера версии. Номера в истории переиспользовались (таблица версий дважды сбрасывалась), поэтому для номера, публиковавшегося несколько раз, указана последняя публикация.

## 1.5.5 - 2026-08-22

Тема релиза: вылеты через 10-15 минут после установки модов, самопочинка update.rpf, диагностика ПК.

- **Fix**: броня от донора больше не ломает стриминг - файлы приводятся к читаемому виду до переименования, а нечитаемые не ставятся вовсе. См. [entry-gate](streaming/entry-gate.md).
- **Fix**: из слотов улучшений вычищаются пустышки, из-за которых игра падала на загрузке ресурсов. Разбор таких падений - [crash-triage](streaming/crash-triage.md).
- **Fix**: `update.rpf` чинится сам - снятые моды больше не оставляют мёртвых объявлений, которые игра ищет на каждом заходе.
- **Fix**: интерьеры магазинов - очистка лишних записей больше не рвёт привязки объектов к комнатам.
- **Fix**: настройки игры - пресет не привозит чужой монитор и не выкручивает воду за пределы движка, свои PRO-пресеты снова узнаются.
- **New**: диагностика ПК показывает все мониторы с настоящими именами, реальные слоты памяти и оценку видеокарты, памяти и диска. См. [pcdiag](pcdiag/index.md).

## 1.5.4 - 2026-08-19

Тема релиза: каталог кастомных скинов и ганпаков целиком, крупные превью, улучшения из наборов.

- **Fix**: каталог кастомных скинов и пользовательских ганпаков открывается целиком - раньше показывались только первая тысяча скинов и первые 500 паков.
- **New**: превью скинов в карточках стали крупнее; у скинов из чужих ганпаков картинка больше не пропадает.
- **Fix**: улучшения ставятся из наборов и перестали блокировать друг друга без причины. См. [improvements](improvements/index.md).
- **Infra**: проверка загрузок больше не показывает адреса узлов раздачи.
- **Fix**: счётчики сборок и замен, кнопка включения режима Rockstar.

## 1.5.3 - 2026-08-18

Тема релиза: краши после снятия модов, тупик режима Rockstar, вечный поиск GTA, дым на редуксах.

- **Fix**: вылеты при заходе в игру после снятия или смены модов - лаунчер сам вычищает следы чужих установщиков.
- **Fix**: режим Rockstar теперь выключается и после переноса игры на другой диск; при включении лаунчер предупреждает, если запущен Rockstar Games Launcher. См. [hotswap](hotswap/index.md).
- **Fix**: первый экран больше не зависает на поиске GTA 5 и видеокарты.
- **Fix**: дым ставится и на сборках, где он уже заменён.
- **Fix**: игра во второй папке определяется правильно. См. [gta-detection](paths/gta-detection.md).

## 1.5.2 - 2026-08-17

Тема релиза: редукс не пропадает после улучшений, большая карта ставится целиком.

- **Fix**: редукс больше не пропадает после установки улучшений - раньше он оставался в игре, но не грузился.
- **Fix**: улучшения больше не затирают графику редукса своими ванильными файлами.
- **Fix**: большая карта ставится целиком, без ошибки «17 из 18 файлов».
- **Fix**: установки пачкой не падают на занятом файле - теперь ждут и продолжают. См. [mutex](inject/mutex.md).
- **Fix**: в «Загрузках» крутится только то улучшение, которое снимаешь, а не все сразу.

## 1.5.1 - 2026-08-17

Тема релиза: подача деревьев в каталоге.

- **New**: у карточки дерева открывается видеообзор с полноценным плеером.
- **New**: список деревьев тасуется один раз за запуск лаунчера, чтобы одни и те же паки не висели наверху.

## 1.5.0 - 2026-08-16

Тема релиза: логотипы авто, большая карта в углу без цифр, обвесы не торчат из кастомных пушек.

- **New**: карточка «Логотипы авто» в разделе «Другое» - ставит настоящие значки марок на машины вместо выдуманных.
- **Fix**: большая карта по клавише Z снова открывается в левом нижнем углу, куда бы ни была передвинута миникарта.
- **Fix**: из большой карты убраны цифры здоровья и брони - они рассчитаны на маленький радар и висели поверх местности.
- **Fix**: кастомные пушки - ванильные обвесы больше не торчат из модели: закрыты дульные тормоза Mk II, фонари, глушители и дополнительные стволы. См. [glass-and-attachments](gunsmith/glass-and-attachments.md).

## 1.4.9 - 2026-08-16

Тема релиза: деревья одним списком, галерея и счётчик у улучшений, докачка вместо перекачки.

- **New**: деревья и растительность из каталога теперь в одном списке с паками - карточка, галерея кадров и статус установки.
- **New**: у улучшений появилась галерея кадров и счётчик установок.
- **Fix**: прерванная загрузка докачивается, а не начинается заново; лимит времени считается от размера файла, скачанное переживает смену зеркала.
- **Fix**: ганпаки больше не виснут на медленном канале - ограничение скорости работает на обоих проходах.
- **Fix**: режим Rockstar - возврат чистых файлов не конфликтует с проверкой лаунчера, убрана ложная тревога о повреждении.
- **Fix**: сброс кастомизаций и статус улучшений в разделе «Разное».

## 1.4.8 - 2026-08-15

Тема релиза: откат всех модов трогает улучшения, «Разное» не виснет на пропавшем из каталога.

- **Fix**: «Откатить все моды» снимает и улучшения (заправки, светильники и прочее), а не только редукс и ганпак; снятие последнего улучшения возвращает контейнер к чистому виду.
- **Fix**: раздел «Разное» больше не блокируется целиком, если один из установленных модов пропал из каталога.
- **New**: в раздел «Деревья» добавлена подборка цветущих модов.
- **Fix**: положение всплывающих уведомлений об установке.

## 1.4.7 - 2026-08-14

Тема релиза: откат деревьев и дорог не сносит штатные DLC, рюкзаки и звуки.

- **Fix**: откат «Деревьев и дорог» больше не удаляет штатные файлы игры - раньше он мог снести дополнения, из-за чего пропадала часть одежды и приходилось проверять целостность. Если возвращать нечего, откат честно об этом сообщает и ничего не трогает. См. [rollback](inject/rollback.md).
- **Fix**: «Откатить все моды» снова возвращает звуки и штатный `patchday18ng`.
- **Fix**: удаление рюкзаков работает - ставится тот же файл, что и вручную, весит 5 МБ вместо 582 МБ, виден в списке установленного; чужое удаление рюкзаков распознаётся, предлагается возврат.
- **Fix**: больше не появляется ложное «обнаружен сторонний редукс» после обычной установки.
- **Fix**: раздел «Другое» пересканируется при каждом заходе, состояние всегда актуальное.
- **Fix**: сборка миникарты при сбое показывает настоящую причину, а не «файл не похож на миникарту».

## 1.4.6 - 2026-08-13

Тема релиза: прогресс обновления, каталог кастомных без лагов, раздел «Разное».

- **New**: окно обновления показывает ход загрузки - полосу и проценты. Раньше на весь установщик крутилась одна надпись «Скачиваем», и это выглядело как зависание.
- **Fix**: каталог кастомных скинов больше не подвисает при открытии - карточки подгружаются по мере прокрутки.
- **New**: фильтры каталога кастомных - по оружию, по автору и сортировка по скачиваниям.
- **New**: в «Окружении» вместо «Фонарей» раздел «Разное»: цветочные поля и уличный свет.
- **New**: на карточках улучшений видно число установок, у заправок и уличного света появились превью.
- **Fix**: раздел «Другое» разложен без пустот.

## 1.4.5 - 2026-08-13

Тема релиза: версия лаунчера снова честная, локализация движка вернулась в репозиторий.

- **Fix**: лаунчер больше не предлагает обновиться на уже установленную версию. Начиная с 1.4.3 он после обновления продолжал считать себя 1.4.2, поэтому окно обновления возвращалось при каждом запуске, а на обязательной 1.4.3 - ещё и без кнопки «Позже». См. [installed-version-marker](update/installed-version-marker.md) и [loop-incident](update/loop-incident.md).
- **Infra**: немецкий язык временно убран из списка - перевод дорабатывается, у выбравших его интерфейс переключается на английский.
- **Infra**: частичное обновление у 1.4.5 отключено: оно качало больше, чем полный установщик.
- **Infra**: словари локализации движка вернулись в репозиторий.

## 1.4.4 - 2026-08-13

Тема релиза: откат миникарты доведён до конца, раздельные цвета полос.

- **Fix**: откат миникарты возвращает все копии, а не только две стандартные - у тех, чья сборка держит миникарту в своём файле, после «Откатить» оставалась кастомная. Перед заменой делается точка возврата, а при неудаче лаунчер честно сообщает об этом вместо «Готово». См. [minimap-history](customize/minimap-history.md).
- **New**: у пустой части полос HP и брони появился свой цвет - раньше она всегда была темнее заливки на 78 процентов и при белом HP выглядела чёрной.
- **New**: цвета HP и брони настраиваются раздельно, выбранный цвет сразу виден в предпросмотре.

## 1.4.3 - 2026-08-13

Тема релиза: режим Rockstar перестал выбрасывать моды, три кнопки 1.4.2 подключены.

- **Fix**: режим Rockstar больше не выбрасывает моды, когда Rockstar Games Launcher переписывает игру на месте при проверке целостности - раньше это отправляло сборку в мусор и требовало переустановки. См. [agent-and-journal](hotswap/agent-and-journal.md).
- **Fix**: кнопка «Вышел из игры» теперь всегда возвращает чистые файлы; пересборка образа и предупреждение о запущенной игре больше не срабатывают вслепую.
- **Fix**: своё сохранение миникарты, журнал в настройках и выбор миникарты при установке сборки - в 1.4.2 эти три кнопки не были подключены к мосту и ничего не делали.
- **Fix**: дым - `core.ypt` ищется по содержимому, а не по трём жёстко заданным путям.

## 1.4.2 - 2026-08-11

Тема релиза: вернулись пропавшие пушки, звуковые паки под защитой режима Rockstar.

- **Fix**: выборочная установка - каталог обрывался на первой тысяче, из 2421 ствола показывалась только тысяча.
- **New**: при установке сборки лаунчер спрашивает, чью миникарту оставить - свою или из сборки.
- **New**: режим Rockstar - звуковые паки тоже под защитой, агент успевает подставить моды до старта игры, обновление игры больше не путается с проверкой целостности. См. [methods](hotswap/methods.md).
- **New**: мастерская - своё сохранение миникарты в один слот, центровка цифр по полосе, шаг настроек по одному проценту.
- **New**: журнал прицела и раздела «Другое» в настройках - его можно скопировать и прислать, если что-то не поставилось.
- **Fix**: ганпаки - 3D-осмотр открывается кликом по всей карточке; свой шрифт сборки больше не сбрасывается; выход из аккаунта спрашивает подтверждение.

## 1.4.1 - 2026-08-10

Тема релиза: открыты конструктор прицела и редактор миникарты.

- **New**: конструктор прицела и редактор миникарты доступны пользователям. См. [editor](gunsmith/editor.md).
- **Fix**: лимит слотов больше не мешает создавать скины - он проверяется только при сохранении и публикации.
- **Fix**: понятные ошибки вместо номеров, если в игре не хватает файла для стандартного ствола.

## 1.4.0 - 2026-08-10

Тема релиза: мастерская, свои миникарта и прицел, режим Rockstar, сверка загруженного по контрольной сумме.

- **New**: мастерская - свои скины и свои ганпаки. См. [gunsmith](gunsmith/index.md).
- **New**: своя миникарта: цвета, шрифты, положение, свои картинки; конструктор прицела.
- **New**: режим Rockstar - моды переживают проверку целостности Rockstar Games Launcher: файлы игры на диске остаются чистыми, моды подставляются перед стартом. См. [hotswap](hotswap/index.md).
- **New**: раздел авторов модов и журнал загрузок.
- **New**: проверка мода на подмену. См. [legit-check](protection/legit-check.md).
- **Infra**: загрузка модов с ближайшего узла раздачи; всё скачанное сверяется по контрольной сумме перед установкой.

## 1.3.6 - 2026-07-16

Тема релиза: 3D-просмотр векторной карты и переработанная установка трейсеров.

- **New**: большую векторную карту можно рассмотреть в объёме до установки - сцена вращается, приближается колёсиком, перемещение на WASD. Обложки карт в каталоге заменены объёмными превью. См. [glb-viewer](3d/glb-viewer.md).
- **Fix**: трейсер переносится целиком - вместе с привязками к оружию, геометрией и регионами луча и звуковыми метаданными выстрела. Раньше движок переносил эффект частично, и выбранный трейсер мог подмениться стандартным. См. [tracers](history/tracers.md).

## 1.3.5 - 2026-07-15

Тема релиза: раздел «Окружение», чёрный экран при запуске, фон миникарты 5:3.

- **New**: раздел «Окружение» - единое место для модификаций мира: небо (таймциклы), дороги, деревья.
- **Fix**: устранён чёрный экран при запуске, из-за которого у части пользователей лаунчер открывался без интерфейса.
- **Fix**: фон миникарты больше не пропадает при положении «5:3, по центру» с включённой опцией «оставить фон».

## 1.3.4 - 2026-07-12

Тема релиза: определение установленных модов по диску и безопасная сверка состояния.

- **New**: ввод HNT-кода прямо в «Загрузках» - до установки виден подробный состав сборки, установка идёт одним процессом с общим прогрессом, а в установленном показывается одной карточкой. См. [hnt](hnt/index.md).
- **New**: лаунчер сам определяет установленные фаст заход, залазы и зелёные зоны по файлам на диске.
- **New**: управление кешем модов в настройках - отключение, смена диска, очистка. См. [asset-cache](network/asset-cache.md).
- **New**: положение миникарты видно в «Сейчас установлено» и откатывается отдельной кнопкой.
- **Fix**: «Откатить всё» стал быстрее, не нагружает компьютер и показывает прогресс.
- **Fix**: выборочные оружия больше не пропадают из установленного после перезапуска лаунчера.

## 1.3.3 - 2026-07-11

Тема релиза: частичное самообновление лаунчера и сохранение состояния при обновлении.

- **New**: лаунчер обновляется частями - докачиваются только изменённые файлы, а не весь лаунчер заново.
- **Fix**: моды, вход в аккаунт и настройки больше не слетают после обновления.
- **Fix**: установка стала честнее - при неудаче показывается ошибка, а не «успешно» при отсутствии модов в игре.
- **Fix**: выбранное оружие больше не сбрасывается к стандартному из-за разрыва сети.
- **Fix**: большие карты и броня качаются с автопереключением на резервные узлы и больше не виснут на месте.
- **Fix**: точнее определяется, включён ли обход блокировок; добавлена проверка целостности загрузок.

## 1.3.2 - 2026-07-11

Тема релиза: отзывы и рейтинги для больших карт.

- **New**: отзывы и оценки для больших карт, рейтинг на карточках, сортировка каталога по рейтингу и скачиваниям.

## 1.3.1 - 2026-07-11

Тема релиза: установка векторных карт 5RP и переработанная страница карты.

- **Fix**: векторные карты формата 5RP ставятся корректно - раньше могла остаться стандартная карта.
- **New**: страница карты - одно фото или галерея скриншотов и понятное предупреждение перед установкой.
- **New**: установленная большая карта видна в разделе «Загрузки» и убирается одной кнопкой.

## 1.3.0 - 2026-07-11

Тема релиза: выбор источника загрузки EU или RU через сеть раздачи.

- **New**: загрузка модов и установка через сеть узлов - выбор региона EU или RU, подбор самого быстрого узла и очередь при высокой нагрузке.
- **Fix**: устойчивые загрузки при блокировках - обход и резервные зеркала. См. [fragmenting-http-handler](network/fragmenting-http-handler.md).

!!! note "Ниже — нумерация до сброса"

    Четыре записи 1.2.7-1.2.4 относятся к нумерации, которая действовала
    до сброса таблицы версий, поэтому по датам они старше идущих выше
    1.2.3-1.3.0. Номера совпадают, содержимое разное.

## 1.2.7 - 2026-07-04

Тема релиза: разделение лаунчера на пользовательскую и админскую сборки.

- **Infra**: из пользовательской сборки убран неиспользуемый админский код - лаунчер стал легче. См. [publish](build/publish.md).

## 1.2.6 - 2026-07-04

Тема релиза: «Убрать трайсер» переписан на weapons.meta.

- **New**: трассеры убираются у всего оружия - обычного, Mk II, снайперок и транспорта.
- **New**: можно выбрать, у какого оружия убрать, с опциями «Оставить снайперки» и «Убрать везде».
- **New**: в «Загрузках» видно, какие трассеры убраны.

## 1.2.5 - 2026-07-03

Тема релиза: функция «Убрать трайсер» и админ-контроль компонентов редукса.

- **New**: «Убрать трайсер» в разделе «Другое» - `ptfx_bullet_tracer` в `core.ypt` заменяется прозрачной текстурой поверх редукса, оригинал кешируется для отката, изменения переприменяются при смене миникарты или редукса.
- **New**: админ-контроль компонентов редукса - запрет экспорта в чужую персонализацию (`exportBlocked`) и запрет персонализации внутри редукса (`personalizeBlocked`).

## 1.2.4 - 2026-07-02

Тема релиза: функция «Вернуть дым».

- **New**: «Вернуть дым» в разделе «Другое» - смок-текстуры в `core.ypt` заменяются поверх редукса, с автопереприменением при смене миникарты или редукса и откатом в «Загрузках».
- **Fix**: выравнивание отступов карточек без скролла.

## 1.2.3 - 2026-07-10

Тема релиза: вылет игры после персонализации графики.

- **Fix**: доноры без своего файла облаков ломали погоду, и игра вылетала при загрузке. Теперь погода не затирается, а изменения таймцикла применяются корректно.

## 1.2.2 - 2026-07-10

Тема релиза: залазы по серверам, зелёные зоны, фаст заход.

- **New**: залазы - выбор сервера: GTA5RP или Majestic (для Majestic - залазы, запретки и мапинг).
- **New**: зелёные зоны.
- **New**: фаст заход - вход в игру без камеры спавна.
- **New**: лаунчер находит и убирает чужие залазы и зелёные зоны, установленные не через него.
- **New**: поиск по пушкам при сборке связки.
- **Fix**: если `update.rpf` занят другим процессом, лаунчер показывает, какая программа его держит. См. [mutex](inject/mutex.md).
- **Fix**: установка части ганпаков.
- **Infra**: обновлён шрифт интерфейса.

## 1.2.1 - 2026-07-09

Тема релиза: слияние таймцикла по модификаторам, сортировки и переработанный выбор донора.

- **Fix**: таймцикл донора сливается на уровне модификаторов - набор имён, их количество и порядок в базе сохраняются байт в байт, подставляются только значения одноимённых модификаторов. Раньше донорский таймцикл рассинхронизировал таблицу MP-модификаторов и ронял игру.
- **New**: пользовательские сборки - поиск, сортировка (новые / рейтинг / скачивания) и счётчики; сортировка списка пушек.
- **New**: размер и положение миникарты вынесены в раздел «Другое» - работают на любой установленной сборке, персонализация редукса не нужна.
- **New**: выбор донора - поиск, счётчик доноров, сортировка «Популярные».
- **New**: на экране «Загрузки» состав кастомизации раскрывается прямо в карточке.
- **New**: подтверждение переноса брони при переходе на кастомизацию другого редукса.

## 1.2.0 - 2026-07-09

Тема релиза: честная установка вместо ложного «успешно», права администратора, сторожа загрузок.

- **New**: лаунчер запускается от имени администратора - исчезли ошибки доступа при установке.
- **Fix**: если файлы не удалось подготовить, лаунчер сообщает об ошибке и откатывает изменения; раньше писало «успешно», а модов в игре не было.
- **Fix**: ошибка установки брони «There is no Unicode byte order mark» - разбор `content.xml` терпим к BOM.
- **Fix**: шрифты миникарты больше не пропадают при переносе на новый редукс.
- **Fix**: установка миникарты больше не подвешивает компьютер.
- **Fix**: зависшие соединения обрываются за 30 секунд и переключаются на резервный узел.
- **Fix**: при изменённых файлах GTA показывается понятное окно подтверждения, карточка установки больше не виснет.
- **New**: считаются установки ганпаков и брони из каталога. См. [gunpacks](guns/gunpacks.md).
- **Fix**: установщик - зависание на 5%.

## 1.1.9 - 2026-07-09

Тема релиза: вылет ERR_GEN_ZLIB_2 после персонализации, честные ошибки прицела и миникарты.

- **Fix**: вылет GTA при запуске (`ERR_GEN_ZLIB_2`) после персонализации сборки с импортом прицела или других компонентов.
- **Fix**: если изменения прицела или миникарты применить не удалось, лаунчер сообщает об ошибке и откатывает файлы вместо ложного «Готово»; при частичной ошибке показывается окно с причиной.
- **Fix**: цвета миникарты - недостающие компоненты для перекраски докачиваются, цвета больше не пропадают молча.
- **Fix**: зависшее скачивание больше не блокирует очередь установок навсегда; добавлен повторный проход по зеркалам.

## 1.1.8 - 2026-07-07

Тема релиза: запись update.rpf при точечных правках, тихое обновление, сторожа загрузок.

- **Fix**: выборочное оружие не появлялось в игре при установке без ганпака.
- **Fix**: прицелы, миникарта и звуки оружия - исправлена запись `update.rpf`, моды корректно грузятся на строгих серверах. См. [archivefix](rpf/archivefix.md).
- **Fix**: установка брони больше не выглядит зависшей, повторные установки заметно быстрее за счёт кеша контрольных сумм.
- **Fix**: лаунчер больше не замирает во время установки сборок - мост уведён с UI-потока.
- **Fix**: автопереключение на резервный узел, если канал задушен.
- **New**: обновление лаунчера стало тихим - без окна установщика и двойного запуска.
- **Fix**: установщик - зависание на «Проверяю Zapret» и вечная загрузка на медленных узлах; защита от чужого WinDivert, запуск в одном экземпляре.

## 1.1.7 - 2026-07-07

Тема релиза: быстрое переключение на резервный узел и уборка кеша.

- **Fix**: если основной узел не отдаёт файлы, лаунчер переключается на резервный через 15 секунд - подготовка больше не зависает на 0%.
- **Infra**: автоочистка лишних временных и резервных файлов, чтобы кеш не забивал диск.

## 1.1.6 - 2026-07-07

Тема релиза: ганпаки на строгих серверах, центр и цвета миникарты.

- **Fix**: ганпаки корректно ставятся на строгих серверах (Majestic, RAGE-MP) - пушки грузятся в игре.
- **Fix**: расположение миникарты «по центру» работает для всех форматов экрана.
- **New**: отдельный тумблер смены цветов миникарты - позицию можно менять, не трогая цвета.
- **Fix**: после кастомизации на вкладке «Загрузки» мог показываться неверный редукс.

## 1.1.5 - 2026-07-07

Тема релиза: ArchiveFix действительно чинит RPF.

- **Fix**: архив пересобирается через открытие и перезапись, поэтому шифруется и получает контрольные суммы, а не пропускается как «уже готовый». Итог - валидные RPF на строгих мультиплеер-платформах. См. [archivefix](rpf/archivefix.md).
- **Fix**: определение названия брони при подготовке персонализации.

## 1.1.4 - 2026-07-07

Тема релиза: миникарта и прицел с любого редукса, зависания скачивания.

- **New**: миникарту и прицел можно брать с любого редукса; добавлена настройка положения миникарты.
- **Fix**: зависание скачивания модов и установщика.
- **Fix**: повреждённые файлы игры больше не блокируют установку - чистая версия подгружается сама.
- **Fix**: после обновления не слетают вход и список установленных модов.
- **Fix**: понятное сообщение, если обход блокировок уже настроен.

## 1.1.3 - 2026-07-07

Тема релиза: состояние и сессия переживают обновление.

- **Fix**: обновление больше не стирает данные приложения - вход и список установленных модов остаются на месте.
- **Fix**: кеш архива персонализации больше не отдаёт чужой патч.
- **Fix**: убраны ложные ошибки подготовки при резервном копировании - таймауты моста подняты.
- **New**: переводы интерфейса конструктора сборок: английский, немецкий, польский.

## 1.1.2 - 2026-07-06

Тема релиза: RAGE Multiplayer, отдельные пушки, соединение с сервером.

- **Fix**: загрузка модов в RAGE Multiplayer - добавлена папка ключей и починен порядок обработки вложенных архивов (сначала дочерний, потом родительский).
- **Fix**: установка отдельных пушек без ганпака - шаблон DLC берётся с зеркала, если основной источник недоступен.
- **Fix**: устранены ошибки соединения с сервером у части игроков.
- **Fix**: повышена стабильность резервного копирования - повтор при временных сбоях.

## 1.1.1 - 2026-07-06

Тема релиза: критичный хотфикс обхода блокировок, прогресс чистого обновления.

- **Fix**: настройка обхода блокировок не трогает уже установленный Zapret - у части пользователей он переставал запускаться после установки. См. [zapret](network/zapret.md).
- **Fix**: установка чистого обновления показывает реальный прогресс и больше не «висит» на 55%.
- **Fix**: сборки можно сохранять с одним выборочным оружием, без базового ганпака.

## 1.1.0 - 2026-07-06

Тема релиза: тихая автонастройка обхода блокировок в установщике для РФ и РБ.

- **New**: установщик определяет регион клиента и для РФ и РБ сам ставит или обновляет обход блокировок (Zapret), сохраняя пользовательские домены и адреса; загрузки и обновления работают без VPN. Остальных регионов это не касается. См. [zapret](network/zapret.md).
- **New**: установщик запрашивает права один раз и запускает приложение без повышения.
- **Fix**: ускорена и стабилизирована загрузка в России.

## 1.0.13 - 2026-05-25

Тема релиза: фикс рендера у админов, добавление версий редукса без полной перезаливки, починка картинок в РФ.

- **New**: кнопка `+` в «Версиях редукса» в админ-редакторе. Клик ведёт на форму, где выбираешь новый `update.rpf`, парсер его читает, версия добавляется к существующему редуксу (V2, V3, ... до V5). Каталог-row остаётся как есть, только мерджатся `components`. Полная заливка нового редукса не нужна.
- **New**: переключатель проекта (Majestic / 5RP / Both) в окне «Редактирование редукса». Раньше для смены проекта приходилось перезаливать весь патч. Теперь это просто чекбокс, сохраняется в `redux_items.supported_servers`.
- **New**: автозеркалирование внешних картинок при сохранении редукса. Когда админ вставляет URL картинки с imgur / postimg / ibb / любого левого домена в `preview_url` / `galleryUrls` / `componentScreenshots`, при Save лаунчер качает её через `FragmentingHttpHandler`, грузит в наш R2 как `redux/{id}/mirror/{slot}-{sha1}.{ext}` и заменяет URL в БД. После этого картинка идёт через [WebView2 bypass interceptor](network/webview2-bypass.md) и грузится в РФ.
- **New**: автоматическая установка Chromium на машину админа при первом запуске. После скачивания `Renderer.zip` бутстрапер запускает `node node_modules/puppeteer/install.mjs` с `PUPPETEER_CACHE_DIR=Renderer/.cache/puppeteer`. Puppeteer качает `chrome-headless-shell` (~120 MB) с googleapis. Дальше `render.js` использует bundled Chromium 138, а не системный Edge. Это устраняет проблему «у админа работает тестовый рендер, а пакетный падает» из-за залоченных профилей Edge и блокировок Defender.
- **Fix**: дубликат `ProbeRendererState` в `AdminGunpackQueueService` удалён. Теперь и тестовый, и боевой пути используют один `RendererBootstrapper.Probe()`.
- **Fix**: `GlbToPngRenderer.RenderAsync` переписан с наивного `string.Format` quoting на `ProcessStartInfo.ArgumentList` (Win32-safe).
- **Fix**: версии в публичной карточке редукса (V1 / V2 / ...) перенесены из блока описания в карточку установки, под кнопку «Установить». Помещаются до пяти штук в один ряд, длинные label'ы truncate'ятся.

## 1.0.2 - 2026-05-13

**Тема релиза**: bug fixes в кастомизации мульти-версионных redux'ов + проверка settings.xml.

- **Fix**: кастомизация redux'а с 2+ версиями - теперь UI правильно показывает превью компонентов для **выбранной** версии. Раньше превью брались всегда из v1 даже если юзер выбрал v2.
- **Fix**: «Какая версия для customize» - добавлен явный version-picker в UI. Раньше всегда брался первый match по reduxId, что приводило к багам когда у мода больше одной версии.
- **Fix**: «BypassTester сломался - нет файла для теста». Test-file URL мигрировал на новый CDN-путь, теперь BypassTester использует `MiamiGraphicsRenderer_1.0.0.zip` который точно есть в R2.

## 1.0.1 - 2026-05-12

**Тема**: hotfix render error #130.

- **Fix**: React `Error #130` при открытии preview для некоторых redux'ов - react рендерил `undefined` как child из-за async race condition в `useGLTF`. Добавлен loading state.
- **Fix**: minor UI bugs в админ-панели (несохраняющиеся checkboxes).

## 1.0.0 - 2026-05-08

**Тема**: первый production release под брендом Miami Graphics.

- **New**: бренд переименован с «Hunter Graphics» на «Miami Graphics» (codebase остался MiamiGraphics из исторических причин).
- **New**: Customize-flow с multi-version support (`baseVersionId`, `donorVersionId`).
- **New**: HNT-коды для p2p обмена сборками.
- **New**: PRO Players секция в каталоге.
- **New**: GTA Presets - apply settings.xml одной кнопкой.
- **New**: Auto-update через Supabase `app_versions` + Inno installer chain.
- **Infra**: переезд с предыдущего R2 bucket'а на `huntergraphics` с CDN.
- **Network**: финальная версия [FragmentingHttpHandler](network/fragmenting-http-handler.md) (3 фрагмента, no delay) - 92% pass rate в РФ.

## Pre-release (2025)

Не релизилось публично, был beta-период:

- Переход с CodeWalker на RageLib (см. [история](history/codewalker.md)).
- Оптимизация RageLib v1 → v2 (4 мин → 6 сек на инжект, см. [v2](history/ragelib-v2.md)).
- Финальная архитектура shaders для гангов (см. [3D render](history/3d-render.md)).
- WebView2 bypass interceptor.
- Zapret integration.

## Принципы версионирования

- **Patch** (1.0.X) - bugfixes, мелкие UI improvements.
- **Minor** (1.X.0) - новые features (новый component type, новая админ-секция).
- **Major** (X.0.0) - breaking changes в data model. Пока не было.

См. [release-pipeline](build/release-pipeline.md) для процесса.

## Где смотреть детали

- **git log** на [github.com/hntenjoyer/miami-graphics](https://github.com/hntenjoyer/miami-graphics) - все коммиты с описаниями.
- **Supabase `app_versions`** - таблица с `version`, `release_notes`, `released_at`, `is_required`.
- **R2 releases path** - `cdn.miamigraphicsstorage.uk/releases/` - все когда-либо собранные installer'ы.
